# Next.js Site URL Configuration (Used for production canonical metadata & sitemap)
NEXT_PUBLIC_SITE_URL=https://enviroserve.in

# Optional Contact Form Email / CRM Service (Resend / SendGrid / Custom SMTP)
# CONTACT_NOTIFICATION_EMAIL=info@enviroserve.in
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
